name: npm diff

on:
  pull_request:
    types: [opened, assigned]

jobs:
  post-comment:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write
    steps:
      - uses: actions/checkout@v3
      - uses: ./
