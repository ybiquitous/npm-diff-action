name: npm diff

on:
  pull_request:
    types: [opened, assigned]

jobs:
  post-comment:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      issues: write
    steps:
      - uses: actions/checkout@v3
      - uses: ./
